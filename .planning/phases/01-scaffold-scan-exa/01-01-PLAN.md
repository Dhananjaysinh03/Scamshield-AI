---
phase: 01-scaffold-scan-exa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - next.config.mjs
  - postcss.config.mjs
  - tailwind.config.ts
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - .env.example
  - .gitignore
  - README.md
autonomous: true
requirements: [REQ-01]
user_setup: []
must_haves:
  truths:
    - "npm run build succeeds"
    - "Home page shows ScamShield AI brand + intake placeholder region"
    - "Dark theme CSS variables present"
  artifacts:
    - path: package.json
      provides: "Next.js scripts and deps"
    - path: app/page.tsx
      provides: "Brand-first landing shell"
    - path: .env.example
      provides: "Documented env keys"
  key_links:
    - from: app/layout.tsx
      to: app/globals.css
      via: "imports global dark theme"
---

<objective>
Bootstrap Next.js App Router + Tailwind dark shell with ScamShield brand-first home and env template.

Purpose: Unlock all later API/UI work on a compiling base.
Output: Runnable `npm run dev` / `npm run build` project skeleton.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffold-scan-exa/01-CONTEXT.md
@.planning/phases/01-scaffold-scan-exa/01-RESEARCH.md
@.planning/SPEC-SCAMSHIELD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js + Tailwind</name>
  <files>package.json, tsconfig.json, next.config.ts, app/layout.tsx, app/page.tsx, app/globals.css, postcss.config.mjs, .gitignore</files>
  <read_first>.planning/phases/01-scaffold-scan-exa/01-CONTEXT.md, .planning/phases/01-scaffold-scan-exa/01-RESEARCH.md</read_first>
  <action>
Create the Next.js app IN the repo root `D:\CHSC` (not a subfolder). Use non-interactive create-next-app:

`npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias "@/*" --use-npm --turbopack=false`

If directory not empty due to `.planning`/`.git`, manually scaffold equivalent files instead:
- next@15, react@19, tailwindcss, typescript
- `next.config.ts` with `output: 'standalone'`
- App Router `app/` directory
- Do NOT add auth, DB, or UI kit libraries
- Do NOT overwrite `.planning/` or `.git/`
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - package.json contains scripts "dev", "build", "start"
    - app/layout.tsx exists
    - npm run build exits 0
  </acceptance_criteria>
  <done>Build passes on greenfield Next shell</done>
</task>

<task type="auto">
  <name>Task 2: Brand-first dark shell + env example</name>
  <files>app/layout.tsx, app/page.tsx, app/globals.css, .env.example, README.md</files>
  <read_first>.planning/phases/01-scaffold-scan-exa/01-CONTEXT.md</read_first>
  <action>
Replace default Next starter UI with ScamShield shell per CONTEXT D-17..D-19:
- `next/font` expressive pair (e.g. Syne + DM Sans or Geist — NOT Inter/Roboto/Arial)
- CSS variables: dark zinc background, emerald or cyan accent — NO purple gradient theme
- First viewport: brand "ScamShield AI" hero-level, one headline ("Dismantle phishing funnels before they cash out."), one short supporting line, placeholder region `data-testid="intake-region"` for Phase plan 02
- No stat strips, no card grids in hero
- `.env.example` with EXA_API_KEY, OPENAI_API_KEY, ELEVENLABS_API_KEY (commented)
- Minimal README: how to run + env vars + Render note
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - app/page.tsx contains string "ScamShield AI"
    - app/page.tsx contains data-testid="intake-region"
    - .env.example contains EXA_API_KEY
    - globals.css defines --background and --accent (or equivalent CSS variables)
  </acceptance_criteria>
  <done>Dark brand shell builds; intake region reserved</done>
</task>

</tasks>

<verification>
- [ ] npm run build exits 0
- [ ] Brand visible in page.tsx
- [ ] .env.example present
</verification>

<success_criteria>
- Compiling Next.js app at repo root
- Dark brand-first home ready for intake wiring
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-scan-exa/01-01-SUMMARY.md`
</output>
