---
phase: 01-scaffold-scan-exa
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/types.ts
  - lib/heuristics.ts
  - lib/evidence.ts
  - app/api/scan/route.ts
  - app/api/exa/route.ts
  - components/IntakePanel.tsx
  - components/EvidenceList.tsx
  - components/ThreatConsole.tsx
  - components/ScanResults.tsx
  - components/DismantleTeaser.tsx
  - app/page.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements: [REQ-02, REQ-03, REQ-04]
user_setup:
  - service: Exa
    why: Live threat intel (DEMO MODE works without key)
    env_vars: [EXA_API_KEY]
    fallback: Synthetic [DEMO MODE] console lines
must_haves:
  truths:
    - "User can add text drops and files into a session evidence array"
    - "POST /api/scan returns riskLevel, score, urls, signals, summary"
    - "POST /api/exa returns intel lines; DEMO MODE if no key"
    - "Threat console renders [Exa Threat Intel] / [Scan] style lines"
    - "High/critical risk shows disabled Dismantle Attack teaser"
  artifacts:
    - path: app/api/scan/route.ts
      provides: "Scan API"
    - path: app/api/exa/route.ts
      provides: "Exa API with demo fallback"
    - path: components/ThreatConsole.tsx
      provides: "Dark SOC console UI"
    - path: components/IntakePanel.tsx
      provides: "Text + file intake"
  key_links:
    - from: components/IntakePanel.tsx
      to: app/api/scan/route.ts
      via: "client fetch on Scan"
    - from: app/api/scan/route.ts
      to: app/api/exa/route.ts
      via: "client or server follows up per URL"
---

<objective>
Wire evidence intake, heuristic(+optional LLM) scan API, Exa threat intel API, and dark console UI on the Phase 1 shell.

Purpose: Demo-ready Systems C + intake foundation for B; teaser for A.
Output: Working scan → console flow with DEMO MODE without keys.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-scaffold-scan-exa/01-CONTEXT.md
@.planning/phases/01-scaffold-scan-exa/01-RESEARCH.md
@.planning/SPEC-SCAMSHIELD.md
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + heuristics + scan route</name>
  <files>lib/types.ts, lib/heuristics.ts, app/api/scan/route.ts</files>
  <read_first>.planning/phases/01-scaffold-scan-exa/01-CONTEXT.md, .planning/phases/01-scaffold-scan-exa/01-RESEARCH.md</read_first>
  <action>
Implement:
- `EvidenceItem`: `{ id: string, type: 'text'|'file', content: string, filename?: string, createdAt: string }`
- `ScanResult`: `{ riskLevel: 'low'|'medium'|'high'|'critical', score: number, urls: string[], signals: string[], summary: string }`
- `lib/heuristics.ts`: extractUrls(text), scoreEvidence(texts: string[]) per RESEARCH signal list; clamp score 0-100; map bands
- `POST /api/scan` body `{ evidence: EvidenceItem[] }` → ScanResult JSON
- Optional: if process.env.OPENAI_API_KEY, call OpenAI briefly to refine summary (try/catch; heuristics remain source of score if LLM fails)
- No DB. No auth.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - app/api/scan/route.ts exports POST
    - lib/heuristics.ts contains extractUrls
    - ScanResult riskLevel union includes critical
  </acceptance_criteria>
  <done>Scan API compiles and returns structured risk</done>
</task>

<task type="auto">
  <name>Task 2: Exa route with DEMO MODE</name>
  <files>app/api/exa/route.ts, package.json</files>
  <read_first>.planning/phases/01-scaffold-scan-exa/01-RESEARCH.md</read_first>
  <action>
- `npm install exa-js`
- `POST /api/exa` body `{ url: string }` 
- If no EXA_API_KEY: return `{ mode: 'demo', lines: string[] }` with 2-3 realistic but clearly `[DEMO MODE]` prefixed lines about the hostname
- If key present: use exa-js search with numResults: 3, category news, contents highlights (fallback text on error); map to lines prefixed `[Exa Threat Intel]:`
- Never expose API key; never throw uncaught — always JSON 200 with mode live|demo|empty
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - package.json dependencies include exa-js
    - app/api/exa/route.ts contains DEMO MODE or mode: 'demo'
    - app/api/exa/route.ts contains Exa Threat Intel OR [Exa Threat Intel]
  </acceptance_criteria>
  <done>Exa route safe without keys</done>
</task>

<task type="auto">
  <name>Task 3: Intake + console UI wired to APIs</name>
  <files>components/IntakePanel.tsx, components/EvidenceList.tsx, components/ThreatConsole.tsx, components/ScanResults.tsx, components/DismantleTeaser.tsx, app/page.tsx, lib/evidence.ts</files>
  <read_first>app/page.tsx, .planning/phases/01-scaffold-scan-exa/01-CONTEXT.md</read_first>
  <action>
Client components (use client where needed):
- IntakePanel: textarea + file input; append to evidence state; "Add drop" + "Scan now"
- EvidenceList: chronological list of drops (simple, not full timeline graph)
- On Scan: POST /api/scan → show ScanResults; for each url POST /api/exa → append lines to ThreatConsole
- ThreatConsole: dark monospace terminal, auto-scroll, lines as `[Scan]:` / `[Exa Threat Intel]:`
- DismantleTeaser: if riskLevel high|critical, show disabled button "Dismantle Attack" with subtitle "Honeypot arming in Phase 3" — do NOT implement honeypot POSTs
- Mobile-first stack; touch-friendly buttons; focus rings on controls
- Wire into data-testid="intake-region" on page
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - components/ThreatConsole.tsx exists
    - components/IntakePanel.tsx exists
    - components/DismantleTeaser.tsx contains Dismantle Attack
    - app/page.tsx imports IntakePanel or ThreatConsole
  </acceptance_criteria>
  <done>End-to-end UI flow builds</done>
</task>

</tasks>

<verification>
- [ ] npm run build exits 0
- [ ] Curl or node fetch against /api/scan with sample phishing text returns JSON (dev server)
- [ ] /api/exa without key returns demo mode
</verification>

<success_criteria>
- REQ-02/03/04 satisfied for Phase 1 demo
- Missing keys do not break build or happy-path UI
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-scan-exa/01-02-SUMMARY.md`
</output>
