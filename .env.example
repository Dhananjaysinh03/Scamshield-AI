# ScamShield AI — environment
# Copy to .env.local for local development. Never commit real keys.

# Live Exa threat intel (DEMO MODE works without it)
EXA_API_KEY=

# Optional — enriches scan summary + screenshot OCR (vision)
OPENAI_API_KEY=

# Verbal security readouts (stub without key)
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
